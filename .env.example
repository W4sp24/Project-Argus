# Argus configuration — copy to .env and adjust.
# Secrets (API keys, OAuth tokens) do NOT go here; they live in the OS keyring.

# Absolute path to your Obsidian vault (set automatically by `argus init`).
VAULT_PATH=

# Port for the FastAPI backend.
BACKEND_PORT=8000
