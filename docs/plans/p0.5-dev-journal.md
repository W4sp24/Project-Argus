# Phase P0.5 — Build Journal & Obsidian Dev Loop Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Claude Code sessions are journaled into Scientia's dev-owned `90-Meta/` zone (deterministic stubs via hooks, narratives via `/log-session`), and FRIDAY surfaces that journal read-only in a 7th dashboard page plus an Insights activity strip.

**Architecture:** `90-Meta/` is written only by dev-time tooling (hooks + `/log-session`), never by FRIDAY (D1). `backend/journal.py` walks it read-only, honoring `#no-ai` (D2) and confining path input to the zone. Hooks are user-scope PowerShell scripts (`~/.claude/hooks/`) so every project on this machine journals (D3: stubs work with Obsidian closed).

**Tech Stack:** PowerShell 5.1 hooks, Claude Code SessionStart/SessionEnd hook protocol, FastAPI + Pydantic, Next.js + react-markdown + recharts, mcp-obsidian via uvx (Local REST API plugin).

## Global Constraints

- D1 `90-Meta/` dev-owned; FRIDAY never writes/proposes into it. No journal write endpoints.
- D2 read surfaces exclude `#no-ai` notes; path inputs cannot escape `90-Meta/`.
- D3 stubs = deterministic filesystem append (hook); narratives = model, on demand only.
- D4 subscription auth (I5); Obsidian REST key in env/keyring, never committed (I4).
- Repo-shipped copies of user-scope assets live in `claude-integration/` (hooks, command, setup script) so the repo stays open-source usable; installed copies go to `~/.claude/`.

---

### Task 1: Scaffold Scientia — PARA zones + 90-Meta (dev write, outside repo)

- [ ] Non-destructively add the vault-template PARA folders to Scientia (skip files that exist; keep existing `Welcome.md`, root daily note untouched).
- [ ] Create `90-Meta/{sessions/2026/, projects/, _templates/}`, `90-Meta/dashboard.md`, `_templates/{session.md, project.md}`, seed `90-Meta/projects/project-argus.md` (project = repo folder name, so hooks match cwd).
- [ ] `git init` Scientia + initial commit (I2 groundwork, free undo for vault writes).

### Task 2: Hooks — session-start-context.ps1 + session-end-journal.ps1

**Files:** Create `claude-integration/hooks/{session-start-context.ps1,session-end-journal.ps1}`; install copies to `~/.claude/hooks/`; wire `~/.claude/settings.json` (SessionStart matcher `startup|resume`, SessionEnd).

**Contracts:** Both read the hook JSON payload from stdin (`cwd`, `session_id`). Project slug = lowercased basename of cwd. Vault root from `$env:FRIDAY_VAULT` else default `C:\Users\ethan\Documents\Scientia`.
- Start hook: if `90-Meta/projects/<slug>.md` exists → emit `{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"..."}}`.
- End hook: append stub (`## HH:mm session <id-prefix>` + project, cwd, branch, `git diff --name-only HEAD` count) to `90-Meta/sessions/<yyyy>/<yyyy-MM-dd>-<slug>.md`, creating from template with frontmatter if absent. UTF-8 no BOM. Never throws (exit 0 always; journaling must not break sessions).

- [ ] Write scripts; run end hook with a sample JSON payload → stub file appears with correct git fields (exit criterion).
- [ ] Run start hook with sample payload → additionalContext JSON with project note body.
- [ ] Wire settings.json via update-config skill; install copies.

### Task 3: /log-session command

- [ ] `claude-integration/commands/log-session.md` + install to `~/.claude/commands/`: instructs the model to append a `## Narrative` (what was done, decisions, next steps) to today's session note for the current project and refresh the project note's `Open threads`/`Recent sessions`; write via Obsidian MCP when connected, else direct file write; honor `#no-ai`; never write outside `90-Meta/`; always report the vault-relative path written.

### Task 4: MCP registration (partial — key is a human touchpoint)

- [ ] Install uv (`winget install astral-sh.uv`); `claude mcp add obsidian -s user -e OBSIDIAN_API_KEY=REPLACE_WITH_KEY -e OBSIDIAN_HOST=127.0.0.1 -e OBSIDIAN_PORT=27124 -- uvx mcp-obsidian`. Mark BLOCKED-on-key in BUILD_STATE.

### Task 5: backend/journal.py + /api/journal/* (read-only, D2)

**Files:** Create `backend/journal.py`; modify `backend/main.py`; test `tests/test_journal.py`.

**Interfaces:**
- `list_projects(vault) -> list[JournalProject{slug,title,updated,sessions,open_threads}]`
- `list_sessions(vault, project=None) -> list[JournalSession{date,project,branch,files,has_narrative,path}]` (branch/files parsed from newest stub; `has_narrative` = `## Narrative` present)
- `read_note(vault, rel_path) -> JournalNote{markdown, obsidian_uri}`; `JournalPathError` if the resolved path escapes `90-Meta/`; skip any note whose frontmatter tags or body contain `no-ai`.
- Endpoints: `GET /api/journal/projects`, `GET /api/journal/sessions?project=`, `GET /api/journal/note?path=` (→ 400 on traversal, 404 outside/missing). No POST/PUT/DELETE.

- [ ] Failing tests: seeded fixture vault → projects/sessions listed; `#no-ai` session absent from all three endpoints; `path=../Welcome.md` and absolute paths → 400.
- [ ] Implement; ruff; green; commit.

### Task 6: web/journal page + nav + insights strip

- [ ] `npm i react-markdown recharts`; add Journal to Sidebar (7 items, book/pen icon).
- [ ] `web/app/(dashboard)/journal/page.tsx`: two-pane — projects rail (cards), session timeline grouped by day (branch, files count, narrative badge), detail pane rendering markdown with `obsidian://open?vault=Scientia&file=...` chip and a "dev-owned · view only" badge.
- [ ] `web/app/(dashboard)/insights/page.tsx`: add "Development activity" strip — sessions-per-week recharts bar chart + projects-touched + files-changed trend from `/api/journal/sessions`.
- [ ] `npm run build` green; commit.

### Task 7: Exit criteria + docs + merge

- [ ] Run addendum §6 exit criteria (MCP connectivity + /log-session narrative marked BLOCKED on Obsidian REST key); paste evidence in BUILD_STATE; update BUILD_LOG; merge to main; push.
