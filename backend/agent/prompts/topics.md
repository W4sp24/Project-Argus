Finally, after everything above, add one more section:

## Topics

Three to seven concept names, one per line, each prefixed with `- `.

A concept name is the thing itself, as a textbook would title it —
"Determinism", "Linear Regression", "Bayes' Theorem". It is not a sentence,
not a phrase copied out of this document, and not the document's own title.
Name what the document is *about*, not what it says about it: a chapter
arguing against determinism is still about determinism.

Write the section even if it feels redundant with the note above. It is read
by the app rather than by the reader, and it is what connects this note to the
rest of the vault.
